###
# Rename this file to `.env` and fill in your own API keys.
# These values are used by the Express server to call external APIs.

# API key for the news provider (required).
NEWS_API_KEY=your_news_api_key_here

# Base URL for the news API (optional).  Defaults to the NewsAPI endpoint.
# Example: https://newsapi.org/v2/top-headlines
NEWS_API_URL=https://newsapi.org/v2/top-headlines

# Country code for top headlines (e.g. us, gb, ca).  Leave blank to fetch global headlines.
NEWS_API_COUNTRY=us

# API key for the OpenAI summarisation service (optional).
# If omitted or empty the server will return the first couple of sentences of each article as a "summary".
OPENAI_API_KEY=your_openai_api_key_here

# Port the Express server will listen on (default is 5000).
PORT=5000